$(function () {    var winH = $(window).height(),        headerConact = $('.m-header-contact').innerHeight(),        navbar = $('.navbar').innerHeight();    $('.m-slider-header,.carousel-item,.m-slider-header').height(winH - (headerConact+navbar));    // feature work    $('.m-feature-work ul li').on('click',function () {        $(this).addClass('active').siblings().removeClass('active');        console.log($(this).data('class'));        if ($(this).data('class')== 'all'){            $('.m-suffule-image .col-md').css('opacity',1);        }else        {            $('.m-suffule-image .col-md').css('opacity','.09');            $($(this).data('class')).parent().css('opacity',1);        }    });});